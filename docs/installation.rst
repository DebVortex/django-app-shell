============
Installation
============

At the command line::

    $ easy_install django-app-shell

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-app-shell
    $ pip install django-app-shell
